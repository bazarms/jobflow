variables:
  var1: $VAR1
  var2: ${VAR2}

job1:
  tasks:
    - name: shell11
      shell:
        cmd: exec
        params:
          cmd: "echo {{ .context.var1 }}"
    - name: shell12
      shell:
        cmd: exec
        params:
          cmd: "echo {{ .context.var2 }}"

job2:
  tasks:
    - name: shell2
      shell:
        cmd: exec
        params:
          cmd: "echo {{ .context.var1 }}/{{ .context.var2 }}"
